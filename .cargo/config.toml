# Cargo configuration for delaunay project
# All command aliases have been migrated to justfile for better workflow management

# Criterion report generation is configured through its Cargo features and
# benchmark setup, not a CRITERION_OUTPUT_FORMAT environment variable.
