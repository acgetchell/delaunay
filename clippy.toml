# Clippy configuration for the delaunay crate
# This ensures consistent configuration across all developers and IDEs

# Minimum Supported Rust Version - matches AGENTS.md and Cargo.toml
msrv = "1.92.0"

# Note: Lint levels are controlled via command-line flags in AGENTS.md:
# cargo clippy --workspace --all-targets --all-features -- -D warnings -W clippy::pedantic -W clippy::nursery -W clippy::cargo
#
# This file contains configuration options that Clippy supports.
# For IDE consistency, configure rust-analyzer with the same command-line flags.
